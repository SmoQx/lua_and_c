#include <stdio.h>


int main(){
    float rain_per_years[5][12] = {
                                    {},
                                    {},
                                    {},
                                    {},
                                    {}
                                    };

    for (int x = 0; x < sizeof(rain_per_years) / sizeof(rain_per_years[0]); x++) {
        for (int y = 0; y < sizeof(rain_per_years) / sizeof(rain_per_years[0]); y++) {
            
        }
    }
}
